(() => {
  const toggle=document.querySelector('.nav-toggle'),nav=document.querySelector('#main-nav');
  if(toggle&&nav)toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});
  document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());
  document.querySelectorAll('form[data-api]').forEach(form=>form.addEventListener('submit',async e=>{e.preventDefault();const status=form.querySelector('.form-status'),button=form.querySelector('button[type=submit]');if(status)status.textContent='Enviando…';if(button)button.disabled=true;try{const base=form.dataset.apiBase||window.KZ_API_BASE||'';const res=await fetch(base+form.dataset.api,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const json=await res.json().catch(()=>({}));if(!res.ok||!json.success)throw Error(json?.error?.message||'Não foi possível concluir a operação.');if(json.data?.token)localStorage.setItem('kz_session',json.data.token);if(status){status.className='form-status success';status.textContent=form.dataset.success||'Operação concluída com sucesso.';}form.reset();}catch(err){if(status){status.className='form-status error';status.textContent=err.message||'Ocorreu um erro. Tente novamente.';}}finally{if(button)button.disabled=false;}}));
})();